#!/bin/bash

# Shows detailed information about every file in this directory
alias info="ls -ltia"

# Moves the current working directory to its parent directory (or parent's parent directory for up2)
alias up="cd .."
alias up2="cd ../.."

# Prints the current working directory (e.g /c/Users/John Smith)
alias path="pwd"
